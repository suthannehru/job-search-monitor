# State

Each file here (e.g. `waymo.json`) holds the list of job posting URLs/IDs last seen for that company, written by the hourly monitor routine. Used to diff against the next run so only genuinely new postings trigger a notification.
